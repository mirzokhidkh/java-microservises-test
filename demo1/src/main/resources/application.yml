server:
  port: 8081

spring:
  application:
    name: demo1


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
#  instance:
#    hostname: localhost